rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }
    
    // We mandate verified emails for extra security
    function isVerifiedOwner(userId) {
      return isOwner(userId) && request.auth.token.email_verified == true;
    }

    function incoming() {
      return request.resource.data;
    }

    function existing() {
      return resource.data;
    }

    function isValidId(id) {
      return id is string && id.size() > 0 && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    function isValidUserCreate(data) {
      return data.keys().hasAll(["email", "createdAt"])
        && data.keys().size() <= 3
        && data.email is string
        && data.email.size() <= 200
        && data.createdAt == request.time
        && data.get("displayName", "") is string
        && data.get("displayName", "").size() <= 100;
    }

    function isValidUserUpdate(data) {
      return data.get("displayName", "") is string
        && data.get("displayName", "").size() <= 100;
      // email and createdAt cannot change
    }

    // Allows the client connection tests to pass cleanly
    match /test/connection {
      allow read: if true;
    }

    // Allows the UI search, list, and single fetches to access the scraped data
    match /stories/{storyId} {
      allow read: if true;
      allow write: if false; 
    }

    match /users/{userId} {
      allow get: if isValidId(userId) && isOwner(userId);
      allow list: if false; // Users cannot list all basic users
      allow create: if isValidId(userId) && isOwner(userId) && isValidUserCreate(incoming());
      allow update: if isValidId(userId) && isOwner(userId) && isValidUserUpdate(incoming()) && incoming().email == existing().email && incoming().createdAt == existing().createdAt && incoming().diff(existing()).affectedKeys().hasOnly(['displayName']);
      allow delete: if false; // Disallow user deletion directly from client for now

      match /library/{libraryId} {
        allow get: if isValidId(userId) && isValidId(libraryId) && isOwner(userId);
        allow list: if isValidId(userId) && isOwner(userId);
        
        function isValidLibraryItemCreate(data) {
          return data.keys().hasAll(["storyId", "status", "addedAt"])
            && data.keys().size() == 3
            && data.storyId is string
            && data.storyId.size() <= 128
            && data.status in ["READING", "BOOKMARKED", "COMPLETED", "DROPPED"]
            && data.addedAt == request.time;
        }

        function isValidLibraryItemUpdate(data) {
          return data.status in ["READING", "BOOKMARKED", "COMPLETED", "DROPPED"];
        }

        allow create: if isValidId(userId) && isValidId(libraryId) && isOwner(userId) && isValidLibraryItemCreate(incoming()) && exists(/databases/$(database)/documents/users/$(userId));
        allow update: if isValidId(userId) && isValidId(libraryId) && isOwner(userId) && isValidLibraryItemUpdate(incoming()) && incoming().storyId == existing().storyId && incoming().addedAt == existing().addedAt && incoming().diff(existing()).affectedKeys().hasOnly(['status']);
        allow delete: if isValidId(userId) && isValidId(libraryId) && isOwner(userId);
      }
    }
  }
}